import {View, Text} from 'react-native';
import React from 'react';

const GrayBigTitle = props => {
  return <Text style={{fontSize: 25, color: 'gray'}}>{props.title}</Text>;
};

export default GrayBigTitle;
